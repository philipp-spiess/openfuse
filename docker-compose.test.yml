services:
  test-node:
    # Node.js e2e runner with /dev/fuse access
    build:
      context: .
      dockerfile: Dockerfile.test
    cap_add:
      - SYS_ADMIN
    devices:
      - "/dev/fuse:/dev/fuse"
    security_opt:
      - apparmor:unconfined
    command: ["node", "tests/mount.e2e.ts"]
